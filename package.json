{
  "name": "cookie.clicker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm vite",
    "deploy": "pnpm run build  && gh-pages -d dist",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0",
    "prettier": "3.0.3",
    "vite": "^4.4.5"
  }
}
